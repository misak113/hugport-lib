{
  "name": "hugport-lib",
  "version": "1.0.3",
  "main": "./dist",
  "files": [
    "dist",
    "libs",
    "tools",
    "README.md",
    "package.json"
  ],
  "repository": {
    "url": "git@github.com:misak113/hugport-lib",
    "type": "git"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tslint --format checkstyle --config node_modules/hugport-codestyle/tslint.json --out dist/tslint-report.xml {src,tests}/**/*.{ts,tsx}",
    "prebuild": "rm -rf dist/* && rm -rf typings/* && typings install",
    "prepublish": "yarn run prebuild && yarn run build",
    "release": "yarn version --new-version patch && (npm publish ; git push origin master --tags)",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc --watch"
  },
  "author": "Michael Zabka <zabka.michael@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "co": "^4.6.0",
    "mocha-jenkins-reporter": "^0.2.6"
  },
  "peerDependencies": {
    "amqplib": "~0.4.2",
    "mocha": "^3.1.2"
  },
  "publishConfig": {
    "registry": "https://ci.hugport.com:4875",
    "access": "public"
  },
  "devDependencies": {
    "hugport-codestyle": "^0.0.4",
    "typescript": "^2.0.6",
    "typings": "^1.5.0"
  }
}
